// @ts-ignore
import { slimImg } from '@/utils/tool';

export default {
  // async beforeRouteUpdate (to, from, next) {
  //   const {
  //     asyncData,
  //   } = this.$options;
  //   if (asyncData) {
  //     asyncData({
  //       store: this.$store,
  //       route: to,
  //     }).then(next).catch(next);
  //   } else {
  //     next();
  //   }
  // },
  methods: {
    slimImg,
  },
};
